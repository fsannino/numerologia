/**
 * Versão do motor de cálculo, gravada em todo traço (§6 da spec).
 * Se uma regra for corrigida, mapas antigos permanecem reproduzíveis e
 * o usuário é notificado da versão nova — nunca mudam em silêncio.
 */
export const ENGINE_VERSION = '0.1.0'
